{
  "name": "speaklink",
  "private": true,
  "scripts": {
    "build": "npm --prefix client run build",
    "install:all": "npm install --prefix client && npm install --prefix server",
    "start": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "start:prod": "npm --prefix client run build && npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.10.1",
    "jsonwebtoken": "^9.0.3",
    "nodemon": "^3.1.14"
  },
  "allowScripts": {
    "better-sqlite3@12.10.1": true
  }
}
